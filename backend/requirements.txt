# itsmegram Backend Dependencies
# Python FastAPI + 관련 패키지

# FastAPI 및 서버
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.17

# 환경 변수 관리
python-dotenv==1.0.1
pydantic-settings==2.6.1

# HTTP 클라이언트 (외부 API 호출용)
httpx==0.27.2
aiohttp==3.11.2

# 데이터 검증 및 직렬화
pydantic==2.9.2

# Rate Limiting
slowapi==0.1.9

# 캐싱 (Redis)
redis==5.2.0

# 구조화된 로깅
structlog==24.4.0
python-json-logger==2.0.7

# 유틸리티
python-dateutil==2.9.0

# Instagram 직접 HTTP 요청 (Instaloader 제거 대체)
curl_cffi>=0.6.0

# AI 분석 (Moonshot AI)
openai>=1.0.0

# 이미지 생성 (Playwright)
playwright==1.49.1

# 템플릿 엔진
jinja2==3.1.4

# 이미지 처리 (JPG 변환)
pillow==11.0.0
